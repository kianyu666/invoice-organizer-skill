---
name: invoice-organizer
description: 批量整理发票文件：扫描识别、备份、按规则重命名、生成统计表、报销核对。当用户要求整理发票、重命名发票、统计发票金额、核对报销时使用。
---

# 发票整理（invoice-organizer）

## 概览
本技能用于批量整理发票文件：扫描识别目标文件夹中的发票 → 备份 → 按规则重命名 → 生成 Excel 统计表 →（可选）报销核对，兼顾通用性与执行效率。

## 工作流程

Agent 按下述步骤执行，每一步均可选用配套脚本加速。

### 1. 扫描识别
1. 确定目标文件夹路径。
2. 运行 `scripts/scan_invoices.py <目录路径>` 列出目录内疑似发票文件（PDF / 图片）。
3. 结合文件名与文件内容，逐张识别并提取四要素：**类别**、**日期**、**金额**、**人员**，同时标记是否为**替票**。
   - 普通发票：文件名不含「替票」字眼。
   - 替票：文件名含「替票」字眼（如「替票-火车票-xxx」），作为独立类型处理。
4. 将识别结果整理为字段 JSON，供重命名与统计使用，结构如下：
   ```json
   [{"filename": "xxx.pdf", "type": "火车票", "date": "2025-07-19 14:30:52", "amount": 258, "person": "刘宇", "is_ticket": false}]
   ```

### 2. 特殊规则（识别时必须遵守）
- **火车票**：日期以 PDF 内**乘车日期**为准（非开票日期）。
- **行程单**：按行程时间归类。
- **高德打车行程单**：按打车费处理。
- **合并 PDF**：含多张发票、无法单一归类的，**保留原目录不动**，并在结果中提示用户人工处理。

### 3. 备份
运行 `scripts/rename_invoices.py` 时自动完成：重命名前为每张原文件生成 `_备份_` 前缀副本，集中移入各自目录下的「备份」子文件夹。

### 4. 重命名
运行 `scripts/rename_invoices.py <目录路径> <字段.json>`，规则如下：
- 普通发票 → `【发票】类别-日期-金额-人员`
- 替票 → `【替票】类别-日期-金额-人员`
- 重名冲突自动追加 `_2` 后缀（冲突多个则依次 `_3`、`_4`…）
- 日期含时间时使用冒号格式，如 `14:30:52`
- 无法归类（字段缺失）或文件不存在的记录跳过并打印警告，不中断整体流程

### 5. 统计
运行 `scripts/build_stats.py <目录路径> <明细.json|csv> <输出.xlsx>` 生成 Excel 统计表，含两个 Sheet：
- **类型统计**：按类别统计数量与金额合计；**替票为独立类型，不并入普通发票**；金额合计使用 SUMIF 公式动态引用明细 Sheet。
- **发票明细**：逐张列出类型、文件名、日期、金额、人员。

### 6. 报销核对（可选）
1. 导入已报销发票记录。
2. 按「姓名 + 票面金额 + 出行日期（±1 天）」匹配。
3. 结算金额可能含 5 元服务费，需先扣除再比对。
4. 命中后：明细备注列标【已报销】、金额置 0；对应 PDF 移入「已报销」文件夹。

### 7. 增量追加
后续新增发票按同一规则重新整理（扫描 → 重命名 → 追加进统计表），无需重建全部数据。

## 脚本使用说明

| 脚本 | 入参 | 行为 | 依赖 |
| --- | --- | --- | --- |
| `scan_invoices.py` | 目录路径 | 列出发票文件清单，不强行解析内容 | 标准库 |
| `rename_invoices.py` | 目录路径 + 字段 JSON | 备份 + 批量重命名，处理重名冲突 | 标准库 |
| `build_stats.py` | 目录路径 + 明细 JSON/CSV + 输出 xlsx | 生成统计表（SUMIF 动态合计） | openpyxl |

## 注意事项
- 字段缺失或文件不存在时，脚本打印明确错误并跳过，不中断整体流程。
- OCR 识别密集表格（金额 / 日期易错）需人工复核后再重命名。
- 合并 PDF、无法归类的文件保留原目录不动，需人工处理。
- 重命名、移动、删除等物理操作执行前需向用户确认目标与影响范围。
